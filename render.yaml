services:
  - type: web
    name: financial-dashboard
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/transactions
    dockerContext: .
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 3000
