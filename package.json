{
  "name": "financial-dashboard",
  "version": "1.0.0",
  "description": "Simple financial dashboard with CSV persistence",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "packageManager": "npm@10.8.2",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'no build step'",
    "render-build": "npm ci --omit=dev || npm install --production"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "sqlite3": "^5.1.7"
  }
}
